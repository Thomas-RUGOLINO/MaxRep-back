const { User, Session, Sport, Category, Best_performance } = require("./associations");

//We import all our models and export them as an object for usage in associations.js

module.exports = {User, Session, Sport, Category, Best_performance};